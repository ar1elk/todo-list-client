import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#14FFEC',
      dark: '#323232',
    },
    secondary: {
      main: '#0D7377',
      dark: '#212121',
    },
  },
});
